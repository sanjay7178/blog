# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# css
- Tailwind `@apply` directives must always be placed inside a `@layer` block (e.g., `@layer components { ... }`), otherwise production builds will fail. Confidence: 0.70

# package-manager
- Use bun as the package manager instead of pnpm. Confidence: 0.60

